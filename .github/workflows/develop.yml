name: Deploy Inkedfur

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy backend 
    runs-on: ubuntu-latest
    env: 
      SERVER_PASSWORD: ${{secrets.PASSWORD}}
    steps:
      - name: Deploy to server
        run: |
          sshpass -p "$SERVER_PASSWORD" scp -rv /inkedfur-v3-backend-cicd root@45.79.9.153:/root/inkedfur-v3-backend-cicd
    # steps:
    #   - name: Deploy using ssh
    #     uses: appleboy/ssh-action@master
    #     with:
    #       host: ${{ secrets.HOST }}
    #       username: ${{ secrets.USERNAME }}
    #       password: "inkedfur-v3"
    #       port: 22
    #       script: |
    #         cd /root/inkedfur-v3-backend-cicd
    #         git pull -r
    #         npm install